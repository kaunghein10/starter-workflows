name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        959266237511-message: 'Message that will be displayed on users'' first issue'
        959440270381-message: 'Message that will be displayed on users'' first pr'
